"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paginateListModelExplainabilityJobDefinitions = void 0;
const ListModelExplainabilityJobDefinitionsCommand_1 = require("../commands/ListModelExplainabilityJobDefinitionsCommand");
const SageMakerClient_1 = require("../SageMakerClient");
const makePagedClientRequest = async (client, input, ...args) => {
    return await client.send(new ListModelExplainabilityJobDefinitionsCommand_1.ListModelExplainabilityJobDefinitionsCommand(input), ...args);
};
async function* paginateListModelExplainabilityJobDefinitions(config, input, ...additionalArguments) {
    let token = config.startingToken || undefined;
    let hasNext = true;
    let page;
    while (hasNext) {
        input.NextToken = token;
        input["MaxResults"] = config.pageSize;
        if (config.client instanceof SageMakerClient_1.SageMakerClient) {
            page = await makePagedClientRequest(config.client, input, ...additionalArguments);
        }
        else {
            throw new Error("Invalid client, expected SageMaker | SageMakerClient");
        }
        yield page;
        const prevToken = token;
        token = page.NextToken;
        hasNext = !!(token && (!config.stopOnSameToken || token !== prevToken));
    }
    return undefined;
}
exports.paginateListModelExplainabilityJobDefinitions = paginateListModelExplainabilityJobDefinitions;
